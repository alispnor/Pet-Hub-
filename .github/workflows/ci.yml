# Pet Hub — CI placeholder
#
# Esqueleto criado na Fase 0. Cada job ainda só imprime TODO.
# Implementação real:
#   - Fase 1: job `test-backend` real com Maven + Testcontainers
#   - Fase 5: job `test-frontend-storefront` com npm test
#   - Fase 6: job `test-frontend-admin` com npm test
#   - Fase 8: job `build-images` com Docker buildx
#   - Fase 12: scan Trivy + deploy CD em workflows separados

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: TODO - Lint backend e frontends
        run: |
          echo "TODO Fase 1+: cd backend && mvn spotless:check"
          echo "TODO Fase 5+: cd frontend/storefront && npm ci && npm run lint"
          echo "TODO Fase 6+: cd frontend/admin && npm ci && npm run lint"

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - name: TODO - Testes unitarios e de integracao
        run: |
          echo "TODO Fase 1+: cd backend && mvn test"
          echo "TODO Fase 5+: cd frontend/storefront && npm test -- --watch=false"
          echo "TODO Fase 6+: cd frontend/admin && npm test -- --watch=false"

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - name: TODO - Build backend e frontends
        run: |
          echo "TODO Fase 1+: cd backend && mvn package -DskipTests"
          echo "TODO Fase 5+: cd frontend/storefront && npm run build"
          echo "TODO Fase 6+: cd frontend/admin && npm run build"
