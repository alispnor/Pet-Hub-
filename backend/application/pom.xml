<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.alispnor.pethub</groupId>
        <artifactId>pet-hub-backend</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>pet-hub-application</artifactId>
    <name>Pet Hub :: Application</name>
    <description>Spring Boot Application + configuração global</description>

    <dependencies>
        <dependency>
            <groupId>com.alispnor.pethub</groupId>
            <artifactId>pet-hub-common</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alispnor.pethub</groupId>
            <artifactId>pet-hub-identity</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alispnor.pethub</groupId>
            <artifactId>pet-hub-catalog</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alispnor.pethub</groupId>
            <artifactId>pet-hub-customer</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alispnor.pethub</groupId>
            <artifactId>pet-hub-payment</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alispnor.pethub</groupId>
            <artifactId>pet-hub-cart</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alispnor.pethub</groupId>
            <artifactId>pet-hub-pricing</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        </dependency>

        <!-- Postgres + Flyway -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

        <!-- Logging JSON (apenas em prod via profile, mas dep está sempre disponível) -->
        <dependency>
            <groupId>net.logstash.logback</groupId>
            <artifactId>logstash-logback-encoder</artifactId>
        </dependency>

        <!-- Test deps -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>pet-hub-backend</finalName>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>com.alispnor.pethub.PetHubApplication</mainClass>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
